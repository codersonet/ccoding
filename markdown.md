#

open terminal first get into 'cd Documents'
then type 'cd ccoding'
then type 'ls' or 'ls -a' // check how many directroies are there in the c programming folder
use 'pwd' for tracing path of the file location

USE 'nano <filename.c>' to open a new file
use 'clang <filename.c> -o <filename.out>' for the compiler file to exist.

remaining commands work like linus terminal only
